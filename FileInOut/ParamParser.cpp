#include "pch.h"
#include "ParamParser.h"


ParamParser::ParamParser(char* ch)
{
	chWord = new char[MAX_WORD];
	chWord = ch;
}

void ParamParser::CalcParam(char* ch)
{
	//int k, j = 0;
	//for (int i = 0; i < MAX_WORD; i++)
	//{
	//	if (*(ch + i) != *ch)
	//	{
	//		PerWord[k][j++] = *(ch + i);
	//	}

	//	else
	//	{
	//		k++;
	//	}
	//}
}
